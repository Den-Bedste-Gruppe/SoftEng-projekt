#Author: Philip Hviid
Feature: Create Activity
	Description: A project leader checks availibility of all worker in Timeframe
  of a projectactivity
	Actors: Worker

Background:
	Given that a worker is logged in
  Given A project exists
  And Worker is the project leader of current project

Scenario: User checks availibility of other busy worker in a timeframe
  Given worker "ZXCV" is on activity with
